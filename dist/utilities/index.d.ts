/**
 * The utilities of this SDK.
 *
 * For example, you can determine the course state with
 * `ExtractCourseState()`.
 *
 * @module
 */
import ExtractCourseState from "./extract_course_status";
import { getAddedTime, isAfter, isBefore, isTimeOffset, TimeOffset, timeOffsetPattern } from "./is_after";
export { TimeOffset, timeOffsetPattern, ExtractCourseState, isTimeOffset, getAddedTime, isAfter, isBefore, };
//# sourceMappingURL=index.d.ts.map